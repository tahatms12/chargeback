# Legacy infrastructure assets

This directory stores infrastructure artifacts that are preserved for historical or optional local experiments.

- `gateway/nginx.conf` is a legacy NGINX gateway template used by `docs/docker-compose.paths.example.yml`.
- It is **not** used by the active Vercel multi-project deployment model in this repository.
